{
  "name": "decodejpi",
  "version": "0.2.0",
  "description": "TypeScript library to decode JPI (J.P. Instruments) EDM flight data and export CSV",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "decode": "node dist/cli.js",
    "test:golden": "npm run build && node scripts/test-golden.js",
    "test:tripwire": "npm run build && node scripts/test-golden-tripwire.js"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^24.3.1",
    "typescript": "^5.9.2"
  }
}
